<%#<header class="border-b border-gray-200 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60 sticky top-0 z-50 ">%>
<header class="bg-white sticky top-0 z-50 ">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <!-- 로고 -->
      <div class="flex items-center">
        <%= link_to root_path, class: "flex items-center space-x-2" do %>
          <div class="w-8 h-8 bg-gradient-to-r from-orange-400 to-pink-500 rounded-lg flex items-center justify-center">
            <span class="text-white text-lg font-bold">🍰</span>
          </div>
          <span class="text-xl font-bold text-gray-900 hidden sm:block">케이크몰</span>
        <% end %>
      </div>

      <!-- 검색창 (중앙) -->
      <!--
      <div class="flex-1 max-w-2xl mx-4 hidden md:block">
        <div class="relative">
          <form class="w-full">
            <div class="relative">
              <input 
                type="text" 
                placeholder="가게, 음식, 생일로정 검색해보세요..."
                class="w-full px-4 py-3 pr-12 text-gray-900 placeholder-gray-500 bg-gray-50 border border-gray-200 rounded-full focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-transparent transition-all duration-200"
              />
              <button 
                type="submit"
                class="absolute right-3 top-1/2 transform -translate-y-1/2 p-2 text-gray-400 hover:text-gray-600 transition-colors"
              >
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                </svg>
              </button>
            </div>
          </form>
        </div>
      </div>
      -->

      <!-- 모바일 검색 아이콘 -->
      <!-- 
      <div class="md:hidden">
        <button class="p-2 text-gray-600 hover:text-gray-900 transition-colors">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
          </svg>
        </button>
      </div>
      -->

      <!-- 오른쪽 아이콘들 -->
      <div class="flex items-center space-x-3">

        <!-- 가게 등록 버튼 (어드민만) -->
        <% if authenticated? && current_user&.admin? %>
          <%= link_to new_bakery_path, class: "flex items-center space-x-2 px-4 py-2 bg-orange-500 text-white rounded-full hover:bg-orange-600 transition-colors text-sm font-medium" do %>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
            </svg>
            <span>가게 등록</span>
          <% end %>
        <% end %>

        <!-- 위치 아이콘 -->
        <%#<button class="p-2 text-gray-600 hover:text-gray-900 transition-colors relative cursor-pointer">%>
        <%#  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">%>
        <%#    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>%>
        <%#    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>%>
        <%#  </svg>%>
        <%#</button>%>


        <!-- 어드민 메뉴 -->
        <% if authenticated? && !User.where(admin: true).exists? %>
          <!-- 관리자가 없을 때 표시 -->
          <%= link_to new_admin_setup_path, class: "hidden sm:flex items-center px-3 py-1 bg-gradient-to-r from-red-500 to-orange-500 text-white rounded-full text-xs font-medium hover:from-red-600 hover:to-orange-600 transition-all animate-pulse" do %>
            <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-11a1 1 0 10-2 0v2H7a1 1 0 100 2h2v2a1 1 0 102 0v-2h2a1 1 0 100-2h-2V7z" clip-rule="evenodd"></path>
            </svg>
            관리자 설정
          <% end %>
        <% end %>

        <!-- 사용자 메뉴 -->
        <% if authenticated? %>
          <div class="relative">
            <button id="user-menu-button" class="p-1 text-gray-600 hover:text-gray-900 transition-colors relative cursor-pointer">
              <% if current_user.profile_image? %>
                <img src="<%= current_user.profile_image_url %>" class="w-8 h-8 rounded-full object-cover border-2 border-gray-300" alt="">
              <% else %>
                <div class="w-8 h-8 rounded-full bg-gradient-to-br from-amber-400 to-orange-600 flex items-center justify-center text-white text-sm font-bold">
                  <%= current_user.email_address[0].upcase %>
                </div>
              <% end %>
            </button>
            
            <!-- 드롭다운 메뉴 -->
            <div id="user-dropdown" class="hidden absolute right-0 mt-2 w-64 bg-white border border-gray-200 rounded-lg shadow-lg z-50">
              <!-- 프로필 정보 섹션 -->
              <div class="px-4 py-3 border-b border-gray-200">
                <div class="flex items-center space-x-3">
                  <% if current_user.profile_image? %>
                    <img src="<%= current_user.profile_image_url %>" class="w-10 h-10 rounded-full object-cover" alt="">
                  <% else %>
                    <div class="w-10 h-10 rounded-full bg-gradient-to-br from-amber-400 to-orange-600 flex items-center justify-center text-white text-lg font-bold">
                      <%= current_user.email_address[0].upcase %>
                    </div>
                  <% end %>
                  <div class="flex-1 min-w-0">
                    <p class="text-sm font-medium text-gray-900 truncate"><%= current_user.email_address %></p>
                    <% if current_user.admin? %>
                      <p class="text-xs text-amber-600">관리자</p>
                    <% end %>
                  </div>
                </div>
              </div>

              <div class="py-1">
                <% if authenticated? && current_user&.admin? %>
                  <%= link_to admin_root_path, class: "flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50" do %>
                    <svg class="w-3 h-3 mr-3" fill="currentColor" viewBox="0 0 20 20">
                      <path fill-rule="evenodd" d="M6.267 3.455a3.066 3.066 0 001.745-.723 3.066 3.066 0 013.976 0 3.066 3.066 0 001.745.723 3.066 3.066 0 012.812 2.812c.051.643.304 1.254.723 1.745a3.066 3.066 0 010 3.976 3.066 3.066 0 00-.723 1.745 3.066 3.066 0 01-2.812 2.812 3.066 3.066 0 00-1.745.723 3.066 3.066 0 01-3.976 0 3.066 3.066 0 00-1.745-.723 3.066 3.066 0 01-2.812-2.812 3.066 3.066 0 00-.723-1.745 3.066 3.066 0 010-3.976 3.066 3.066 0 00.723-1.745 3.066 3.066 0 012.812-2.812zm7.44 5.252a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                    </svg>
                    관리자 대시보드
                  <% end %>
                  <div class="border-t border-gray-100 my-1"></div>
                <%end%>
                <%= link_to edit_user_path, class: "flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50" do %>
                  <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                  </svg>
                  프로필 수정
                <% end %>
                <%= link_to favorites_path, class: "flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50" do %>
                  <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                  </svg>
                  즐겨찾기
                <% end %>
                <%= link_to all_notes_path, class: "flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50" do %>
                  <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                  </svg>
                  내 노트
                <% end %>
                <div class="border-t border-gray-100 my-1"></div>
                <%= button_to session_path, method: :delete, 
                    class: "flex items-center w-full px-4 py-2 text-sm text-red-600 hover:bg-red-50",
                    data: { turbo_confirm: "로그아웃하시겠습니까?" } do %>
                  <svg class="w-4 h-4 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
                  </svg>
                  로그아웃
                <% end %>
              </div>
            </div>
          </div>
        <% else %>
          <div class="flex items-center space-x-2">
            <%= link_to "로그인", new_session_path, 
                class: "px-4 py-2 text-sm font-medium text-gray-700 hover:text-gray-900 transition-colors" %>
            <%= link_to "회원가입", new_registration_path, 
                class: "px-4 py-2 bg-orange-600 text-white rounded-full text-sm font-medium hover:bg-orange-700 transition-colors" %>
          </div>
        <% end %>
      </div>
    </div>

    <!-- 모바일 검색창 (숨김 상태, 토글 가능) -->
    <div class="md:hidden pb-4 pt-2 hidden" id="mobile-search">
      <div class="relative">
        <input 
          type="text" 
          placeholder="가게, 음식, 생일로정 검색해보세요..."
          class="w-full px-4 py-3 pr-12 text-gray-900 placeholder-gray-500 bg-gray-50 border border-gray-200 rounded-full focus:outline-none focus:ring-2 focus:ring-orange-500 focus:border-transparent"
        />
        <button 
          type="submit"
          class="absolute right-3 top-1/2 transform -translate-y-1/2 p-2 text-gray-400 hover:text-gray-600 transition-colors"
        >
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
          </svg>
        </button>
      </div>
    </div>
  </div>
</header>

<!-- 필터 섹션 (홈페이지에서만) -->
<!--
<% if controller_name == 'home' && action_name == 'index' %>
  <div class="bg-white border-b border-gray-100 py-4">
    <div class="max-w-7xl mx-auto px-4">
      <div class="flex items-center space-x-2">
        <svg class="w-5 h-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"></path>
        </svg>
        <span class="text-sm font-medium text-gray-700">카테고리:</span>
        
        <div class="flex space-x-3 overflow-x-auto">
          <button class="px-4 py-1.5 bg-gray-900 text-white rounded-full text-sm font-medium whitespace-nowrap">
            전체
          </button>
          <button class="px-4 py-1.5 bg-gray-100 text-gray-700 rounded-full text-sm font-medium hover:bg-gray-200 transition-colors whitespace-nowrap">
            베이커리
          </button>
          <button class="px-4 py-1.5 bg-gray-100 text-gray-700 rounded-full text-sm font-medium hover:bg-gray-200 transition-colors whitespace-nowrap">
            카페
          </button>
          <button class="px-4 py-1.5 bg-gray-100 text-gray-700 rounded-full text-sm font-medium hover:bg-gray-200 transition-colors whitespace-nowrap">
            디저트전문점
          </button>
        </div>
      </div>
    </div>
  </div>
<% end %>
-->

<script>
function initializeUserDropdown() {
  const userMenuButton = document.getElementById('user-menu-button');
  const userDropdown = document.getElementById('user-dropdown');
  
  if (userMenuButton && userDropdown) {
    // 기존 이벤트 리스너가 있다면 제거
    userMenuButton.removeEventListener('click', handleUserMenuClick);
    document.removeEventListener('click', handleDocumentClick);
    
    // 새로운 이벤트 리스너 추가
    userMenuButton.addEventListener('click', handleUserMenuClick);
    document.addEventListener('click', handleDocumentClick);
  }
}

function handleUserMenuClick(e) {
  e.stopPropagation();
  const userDropdown = document.getElementById('user-dropdown');
  if (userDropdown) {
    userDropdown.classList.toggle('hidden');
  }
}

function handleDocumentClick(e) {
  const userMenuButton = document.getElementById('user-menu-button');
  const userDropdown = document.getElementById('user-dropdown');
  
  if (userMenuButton && userDropdown && 
      !userMenuButton.contains(e.target) && 
      !userDropdown.contains(e.target)) {
    userDropdown.classList.add('hidden');
  }
}

// DOMContentLoaded와 Turbo 이벤트 모두에 대응
document.addEventListener('DOMContentLoaded', initializeUserDropdown);
document.addEventListener('turbo:load', initializeUserDropdown);
document.addEventListener('turbo:render', initializeUserDropdown);
</script>
