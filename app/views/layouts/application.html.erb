<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "케이크몰" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=5,user-scalable=yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="format-detection" content="telephone=no">
    <meta name="HandheldFriendly" content="true">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <!-- Favicon -->
    <%= favicon_link_tag asset_path('logo/favicon.ico') %>
    <%= favicon_link_tag asset_path('logo/favicon-16x16.png'), sizes: '16x16', type: 'image/png' %>
    <%= favicon_link_tag asset_path('logo/favicon-32x32.png'), sizes: '32x32', type: 'image/png' %>
    <%= favicon_link_tag asset_path('logo/favicon-96x96.png'), sizes: '96x96', type: 'image/png' %>
    
    <!-- Apple Touch Icons -->
    <link rel="apple-touch-icon" sizes="57x57" href="<%= asset_path('logo/apple-icon-57x57.png') %>">
    <link rel="apple-touch-icon" sizes="60x60" href="<%= asset_path('logo/apple-icon-60x60.png') %>">
    <link rel="apple-touch-icon" sizes="72x72" href="<%= asset_path('logo/apple-icon-72x72.png') %>">
    <link rel="apple-touch-icon" sizes="76x76" href="<%= asset_path('logo/apple-icon-76x76.png') %>">
    <link rel="apple-touch-icon" sizes="114x114" href="<%= asset_path('logo/apple-icon-114x114.png') %>">
    <link rel="apple-touch-icon" sizes="120x120" href="<%= asset_path('logo/apple-icon-120x120.png') %>">
    <link rel="apple-touch-icon" sizes="144x144" href="<%= asset_path('logo/apple-icon-144x144.png') %>">
    <link rel="apple-touch-icon" sizes="152x152" href="<%= asset_path('logo/apple-icon-152x152.png') %>">
    <link rel="apple-touch-icon" sizes="180x180" href="<%= asset_path('logo/apple-icon-180x180.png') %>">
    
    <!-- Android Icons -->
    <link rel="icon" type="image/png" sizes="192x192" href="<%= asset_path('logo/android-icon-192x192.png') %>">
    
    <!-- MS Tiles -->
    <meta name="msapplication-TileColor" content="#ffffff">
    <meta name="msapplication-TileImage" content="<%= asset_path('logo/ms-icon-144x144.png') %>">
    <meta name="theme-color" content="#ffffff">
    
    <!-- Manifest -->
    <link rel="manifest" href="<%= asset_path('logo/manifest.json') %>">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <%= render 'shared/header' %>
    
    <main class="flex-1">
      <%= yield %>
    </main>
  </body>
</html>
