<div class="max-w-7xl mx-auto px-4 py-8">
  <!--
  <header class="text-center mb-12">
    <h1 class="text-4xl font-bold text-gray-900 mb-4">서울 맛집 케이크 가게</h1>
    <p class="text-lg text-gray-600 max-w-2xl mx-auto leading-relaxed mb-6">
      달콤한 케이크와 디저트를 만날 수 있는 서울의 인기 베이커리와 카페들을 소개합니다
    </p>
    
    <div class="inline-flex items-center px-4 py-2 bg-gray-100 rounded-full">
      <span class="text-gray-700 font-medium"><%= @bakeries.count %>개의 가게</span>
    </div>
  </header>
  -->
  
  <% if @bakeries.any? %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <% @bakeries.each do |bakery| %>
        <%= render "bakery_card", bakery: bakery %>
      <% end %>
    </div>
  <% else %>
    <div class="text-center py-16">
      <svg class="w-24 h-24 mx-auto text-gray-300 mb-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-4m-5 0H3m2 0h3M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
      </svg>
      <h3 class="text-xl font-semibold text-gray-700 mb-2">아직 등록된 가게가 없습니다</h3>
      <p class="text-gray-500 mb-6">첫 번째 가게를 등록해보세요!</p>
      <% if current_user&.admin? %>
        <%= link_to "가게 등록하기", new_bakery_path, class: "inline-flex items-center px-6 py-3 bg-orange-500 text-white rounded-xl font-medium hover:bg-orange-600 transition-colors" %>
      <% end %>
    </div>
  <% end %>
</div>
